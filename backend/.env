DATABASE_URL=postgresql://postgres:postgres@localhost:5432/finanzas
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
JWT_SECRET=cambia_esto_por_un_secreto_seguro
JWT_EXPIRES_IN=7d

# ── Webhooks — Open Banking ───────────────────────────────────────────────────
# Clave secreta compartida por todos los webhooks de entidades financieras.
# El simulador del frontend la envía en el header X-Webhook-Auth.
WEBHOOK_SECRET=openbanking_dev_secret_2026
